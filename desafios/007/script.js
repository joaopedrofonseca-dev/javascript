function calcular() {
    var n1 = Number(window.prompt('Digite um número inteiro qualquer: '))
    window.alert(`Antes do ${n1}, temos o número ${n1 - 1}.
    Depois do ${n1}, temos o número ${n1 + 1}.
    `)
}